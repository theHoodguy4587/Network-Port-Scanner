# This project uses only the Python standard library (socket, threading).
# No third-party dependencies are required to run `scanner.py`.
# Recommended Python runtime: 3.8+
# If you add external packages later, list them here, e.g.:
# scapy==2.4.5

